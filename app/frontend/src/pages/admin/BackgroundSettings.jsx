import React from "react";

export default function BackgroundSettings() {
  return (
    <div className="space-y-6" data-testid="admin-background-settings">
      <h2 className="text-2xl font-display font-semibold text-slate-900">Background Settings</h2>
      <p className="text-sm text-slate-600">Configure site background (image, gradient, overlay, etc.). Implementation pending.</p>
    </div>
  );
}
